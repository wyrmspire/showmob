# Network inventory: fourth scoped security page

Written by Instinct. The planned sheet in `runs/local-security-lab/pages/local-security-lab-network-inventory/page-sheet.json` preceded representation, draft, critique and the `app/src/content/local-security-lab-network-inventory.json` preview. The page uses the dated baseline as a comparison point, not an intrusion verdict: router client views can be incomplete or stale, local addresses can change, and inaccessible host listeners stay unchecked. It joins the previous owned-host listener exercise with router observations and known offline devices. The learner rehearses one row but saves a repeated, private dated record elsewhere because the page's fields reset.

The new page is scoped order 4 in `runs/local-security-lab/series.json` and app order 5 while the legacy all-in-one page holds order 1. No network scan or configuration change was made. The sheet, draft and critique came from the same author, so the page stays preview for independent review. FTC home-network guidance and CIS asset-inventory guidance are linked on the page. The local phone and desktop render was checked; this is not an accessibility audit.
